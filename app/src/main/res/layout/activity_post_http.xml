<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >


    <EditText
        android:id="@+id/new_post_title"
        android:hint="@string/post_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/new_post_link"
        android:hint="@string/post_link"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/new_post_title"/>

    <EditText
        android:id="@+id/new_post_body"
        android:hint="@string/post_body"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/new_post_link"
        android:gravity="top"/>

    <Button
        android:id="@+id/new_post_submit"
        android:text="@string/submit_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="submitNewPost"
        android:layout_below="@id/new_post_body"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
